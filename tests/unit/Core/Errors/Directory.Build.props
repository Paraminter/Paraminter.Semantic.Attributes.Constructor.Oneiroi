<Project>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <RootNamespace>Paraminter.Associating.Semantic.Attributes.Constructor.Oneiroi.Errors</RootNamespace>
    </PropertyGroup>
    
</Project>